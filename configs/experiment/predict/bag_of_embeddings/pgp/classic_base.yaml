# @package _global_

defaults:
  - /experiment/predict/bag_of_embeddings/classic_base
  - /model/pgp_classic_mil_model@model
  - /model/checkpoints/prostate/pgp_classic_mil@model_data
  - _self_

datamodule:
  predict:
    uris:
      - ${predict_data.tiles_filtered_w_pgp_uri_224}

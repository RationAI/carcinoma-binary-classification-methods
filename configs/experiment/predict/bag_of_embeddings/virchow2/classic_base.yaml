# @package _global_

defaults:
  - /experiment/predict/bag_of_embeddings/classic_base
  - /model/virchow2_classic_mil_model@model
  - /model/checkpoints/mmci_tl/virchow2_classic_mil@model_data
  - _self_

datamodule:
  predict:
    uris:
      - ${predict_data.tiles_filtered_w_virchow2_uri_224}

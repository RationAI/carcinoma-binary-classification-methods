# @package _global_

defaults:
  - /experiment/predict/bag_of_embeddings/hybrid_base
  - /model/virchow2_hybrid_mil_model@model
  - /model/checkpoints/prostate/virchow2_hybrid_mil@model_data
  - _self_

datamodule:
  predict:
    uris:
      - ${predict_data.tiles_filtered_w_virchow2_uri_224}

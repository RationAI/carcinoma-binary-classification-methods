# @package _global_

defaults:
  - /experiment/predict/base
  - _self_

slide_threshold: ${model_data.slide_threshold}
model:
  sl_threshold: ${slide_threshold}

datamodule:
  batch_size: 4
  num_workers: 8

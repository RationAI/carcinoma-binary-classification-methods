# @package _global_

defaults:
  - /experiment/predict/images/base
  - /model/resnet50_model@model
  - _self_

datamodule:
  predict:
    uris:
      - ${predict_data.tiles_filtered_uri_512}
    paths:
      - ${oc.select:predict_data.tiles_filtered_path_512, null}

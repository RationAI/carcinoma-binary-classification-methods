# @package _global_

defaults:
  - /experiment/predict/images/base
  - /model/resnet50_model@model
  - _self_

datamodule:
  predict:
    uris:
      - ${predict_data.tiles_filtered_uri_224}
    paths:
      - ${oc.select:predict_data.tiles_filtered_path_224, null}

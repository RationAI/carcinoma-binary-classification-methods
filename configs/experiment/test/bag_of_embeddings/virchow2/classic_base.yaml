# @package _global_

defaults:
  - /experiment/test/bag_of_embeddings/classic_base
  - /model/virchow2_classic_mil_model@model
  - /model/checkpoints/prostate/virchow2_classic_mil@model_data
  - _self_

datamodule:
  test:
    uris:
      - ${test_data.tiles_filtered_w_virchow2_uri_224}

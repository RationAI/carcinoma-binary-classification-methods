# @package _global_

defaults:
  - /experiment/test/embeddings/base
  - /model/pgp_model@model
  - /model/checkpoints/prostate/pgp_stain@model_data
  - _self_

datamodule:
  test:
    uris:
      - ${test_data.tiles_filtered_w_pgp_uri_224}
    paths:
      - ${oc.select:test_data.tiles_filtered_w_pgp_path_224, null}

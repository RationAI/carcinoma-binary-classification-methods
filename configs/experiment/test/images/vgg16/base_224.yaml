# @package _global_

defaults:
  - /experiment/test/images/base
  - /model/vgg16_model@model
  - _self_

datamodule:
  test:
    uris:
      - ${test_data.tiles_filtered_uri_224}
    paths:
      - ${oc.select:test_data.tiles_filtered_path_224, null}

# @package _global_

defaults:
  - /experiment/test/images/base
  - /model/resnet50_model@model
  - /model/checkpoints/prostate/resnet50@model_data
  - _self_

datamodule:
  test:
    uris:
      - ${test_data.tiles_filtered_uri_512}

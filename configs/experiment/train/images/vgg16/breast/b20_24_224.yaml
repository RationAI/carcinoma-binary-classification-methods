# @package _global_

defaults:
  - /experiment/train/images/vgg16/base_224
  - /data/breast/mmci/b20_24_train@train_data
  - /data/breast/mmci/b20_24_val@val_data
  - _self_

carcinoma_roi_t: ${train_data.thresholds.epithelium_roi_t} # for breast, we use weak epithelium annotations

datamodule:
  train:
    train_pos_tissue_roi_t: ${train_data.thresholds.train_pos_tissue_roi_t}
    num_slides: 400
  val:
    train_pos_tissue_roi_t: ${val_data.thresholds.train_pos_tissue_roi_t}
    num_slides: 100


# @package _global_

batch_size: 1024
output_path: ${carcinoma_artifacts}/tile_embeddings/${tile_encoder.name}/${data.data_name}

tile_encoder:
  _target_: ???

uris: ???
use_case: Tile Embeddings

metadata:
  hyperparams:
    model: ${tile_encoder.name}
    batch_size: ${batch_size}
